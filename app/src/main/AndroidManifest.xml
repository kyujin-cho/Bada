<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Phase 1 + Phase 2 permission set for Quick Share over Wi-Fi LAN
      with BLE auto-discovery.

      Runtime (must be requested at app start, gated by API level):
        * NEARBY_WIFI_DEVICES (API 33+) — required for any local-network
          discovery/advertising. We never derive physical location from peer
          scans, so we set neverForLocation per Google's guidance and skip
          ACCESS_FINE_LOCATION on API 33+ devices.
        * POST_NOTIFICATIONS (API 33+) — used by the foreground transfer
          service notification (#21) and incoming-transfer notifications.
        * BLUETOOTH_ADVERTISE (API 31+, #31) — required to broadcast the
          Quick Share BLE service-data pulse so stock receivers wake up
          their mDNS responder when our app starts a send (#32).
        * BLUETOOTH_SCAN (API 31+, #31) — required to scan for senders'
          BLE pulses so we only enable our mDNS responder when a sender is
          nearby (#33). neverForLocation tells Android we never derive
          physical location from BLE scan results, so the platform skips
          the location-permission requirement.

      Compile-time (no runtime prompt):
        * INTERNET / ACCESS_NETWORK_STATE / ACCESS_WIFI_STATE — local
          socket I/O, network/Wi-Fi state inspection.
        * CHANGE_WIFI_MULTICAST_STATE — required to satisfy the Android
          14+ permission gate on starting a foreground service with type
          `connectedDevice`. The platform requires the app to hold
          FOREGROUND_SERVICE_CONNECTED_DEVICE plus *at least one* of a
          fixed set of "active connection" permissions; the runtime
          BLUETOOTH_ADVERTISE / BLUETOOTH_SCAN we already declare are
          eligible, but only once the user has granted them, which is
          not the case on first launch from the launcher. A normal
          permission like CHANGE_WIFI_MULTICAST_STATE satisfies the gate
          unconditionally and matches our Wi-Fi LAN role. We do not
          actively acquire a MulticastLock — NsdManager handles mDNS in
          the system responder process post-#98 — so the declaration is
          purely for the FGS-type gate.
        * BLUETOOTH / BLUETOOTH_ADMIN (API ≤ 30, #31) — legacy permission
          model for pre-Android-12 devices. Capped with maxSdkVersion=30
          so API 31+ devices use the runtime BLUETOOTH_ADVERTISE /
          BLUETOOTH_SCAN permissions instead.

      BLUETOOTH_CONNECT is declared (API 31+) for the Phase 4 Bluetooth
      bandwidth-upgrade mediums:
        * Bluetooth RFCOMM (#51) — the provider opens an insecure
          RFCOMM listener on the receiver and connects from the sender;
          both calls require BLUETOOTH_CONNECT at runtime on API 31+.
          Pairing is NOT required (we use the *insecure* RFCOMM variants
          because the application data is already UKEY2-encrypted) so
          the user does not see an OS pairing prompt — but the runtime
          permission grant is still mandatory.
        * BLE L2CAP CoC (#52) — BleL2capMediumProvider calls
          BluetoothAdapter.listenUsingInsecureL2capChannel on the
          receiver side and BluetoothDevice.createInsecureL2capChannel
          on the sender side; both require BLUETOOTH_CONNECT at runtime
          on API 31+.
      The :discovery-android library manifest also declares this
      permission at the library level so it propagates into the merged
      manifest automatically; onboarding (#31) prompts for it alongside
      the existing BLE permissions.

      READ_MEDIA_* is intentionally omitted — Phase 1 routes all media
      access through the system share-intent flow, which does not require
      direct media permissions.
    -->

    <!-- Wi-Fi peer discovery on API 33+. neverForLocation tells Android we
         do not infer physical location from these scans, so the platform
         skips the location permission requirement. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />

    <!-- Notification posting on API 33+. Pre-33 devices show notifications
         without a runtime prompt, so the manifest-level entry is gated. -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Phase 2 BLE auto-discovery permissions (#31). Runtime on API 31+.
         BLUETOOTH_SCAN uses neverForLocation so the platform does not
         require ACCESS_FINE_LOCATION; we do not derive physical location
         from BLE pulse scans. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="31" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />

    <!-- Phase 4 Bluetooth RFCOMM bandwidth-upgrade (#51). Runtime on
         API 31+. Pairing is intentionally not required (insecure RFCOMM
         variants); UKEY2 is the security boundary. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31" />

    <!-- Legacy Bluetooth permission model for API ≤ 30. Capped with
         maxSdkVersion=30 so API 31+ devices use the runtime BLUETOOTH_*
         permissions above instead of these grant-at-install variants. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- General networking (compile-time). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Wi-Fi local-only hotspot bandwidth-upgrade medium (#50). Both
         lines are required by WifiManager.startLocalOnlyHotspot on
         API 26+ — CHANGE_WIFI_STATE is install-time normal protection
         level; ACCESS_FINE_LOCATION is the runtime grant the platform
         demands before it will surface the SSID/passphrase in the
         LocalOnlyHotspotReservation callback. The provider in
         :discovery-android also accepts NEARBY_WIFI_DEVICES (already
         declared above for Wi-Fi peer discovery) as a substitute on
         API 33+ devices, so the location grant is requested at
         onboarding only when the platform requires it. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Satisfies the Android 14+ FGS-type gate for `connectedDevice`.
         Normal protection level, so no runtime prompt. We do not actually
         acquire a MulticastLock — NsdManager handles mDNS in the system
         responder process post-#98 — but the platform requires we hold
         at least one permission from a fixed set when starting a
         connectedDevice FGS, and this is the cleanest one that does not
         depend on a runtime grant. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- #47 battery-optimization onboarding. Required for the user to
         actually accept the exemption from the
         ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS dialog the helper
         opens — without this declaration the system Settings activity
         still shows but the toggle is a no-op. The permission is normal
         protection level (no runtime prompt) and must be paired with a
         clear in-app rationale per Google Play policy; the banner copy
         in main_battery_banner_body explains why we ask. The Quick
         Share receiver is foreground-service driven, so the exemption
         is the only way to keep Galaxy / vivo / MIUI background-app
         killers from severing in-flight transfers. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- #145: probe whether a coresident pre-rename WhenVivoMeetsGoogle install
         is still present so the launcher can warn the user to uninstall it. The
         old install registers a duplicate `0xFEF3` GATT advertisement service on
         the same controller, which causes stock Quick Share senders to read the
         slot from one service but open the BLE socket against another (see
         issue #145). Package-visibility filtering on API 30+ silently turns
         `getPackageInfo` into NameNotFoundException for unknown packages, so
         the detector cannot find the legacy install without this carve-out.
         The query is by exact package name only and reveals nothing other than
         "is the legacy WVMG app present?", so the surface stays minimal. -->
    <queries>
        <package android:name="io.github.kyujincho.wvmg" />
        <package android:name="io.github.kyujincho.wvmg.debug" />
    </queries>

    <application
        android:name=".LibreDropApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibreDrop">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".onboarding.PermissionsOnboardingActivity"
            android:exported="false"
            android:label="@string/onboarding_title" />

        <!--
          Sender share-intent target (#24).

          ACTION_SEND (single attachment) and ACTION_SEND_MULTIPLE
          (multiple attachments) are the entry points the system share
          sheet routes to when the user picks our app. We accept */* so
          we appear for every MIME type — file-type restrictions, if
          any, are applied in code so users get a clearer "not
          supported" message than the share sheet hiding us.

          Exported because the share sheet runs in a different process
          (system-ui) and needs to be able to start this activity.
        -->
        <activity
            android:name=".send.SendActivity"
            android:exported="true"
            android:label="@string/send_activity_label">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!--
          QR-code display screen launched from SendActivity (#24 + #20).

          Internal — only SendActivity launches it, so exported=false.
        -->
        <activity
            android:name=".send.ShowQrActivity"
            android:exported="false"
            android:label="@string/show_qr_title" />

        <!--
          Consent trampoline (#22) — the screen-on, lock-screen-bypassing
          dialog opened by tapping the consent notification body.

          Launched only by our own consent notification's content
          PendingIntent (which is package-scoped), so exported=false.
          The activity is documents-style (singleTask + excludeFromRecents)
          so multiple in-flight consents do not pollute the recents
          list — each consent is a transient prompt, not a piece of
          history the user wants to revisit.

          showOnLockScreen / turnScreenOn are also set programmatically
          on API 27+ via Activity setShowWhenLocked / setTurnScreenOn;
          the manifest attributes here are belt-and-braces for the
          launcher hint.
        -->
        <activity
            android:name=".consent.ConsentTrampolineActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:label="@string/consent_activity_label" />

    </application>

</manifest>
